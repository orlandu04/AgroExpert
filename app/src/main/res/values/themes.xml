<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LaterealMenu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- COLORES PRINCIPALES -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorSecondary">@color/accent_color</item>
        <item name="colorSurface">@color/surface_white</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:colorBackground">@color/background_light</item>

        <!-- COLORES ESPECÍFICOS PARA NAVIGATION DRAWER -->
        <item name="drawerArrowStyle">@style/MyDrawerArrowToggle</item>
    </style>

    <style name="Theme.LaterealMenu" parent="Base.Theme.LaterealMenu" />

    <!-- Estilo para el ícono del menú hamburguesa -->
    <style name="MyDrawerArrowToggle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/white</item>
        <item name="spinBars">true</item>
        <item name="drawableSize">24dp</item>
    </style>
</resources>